<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Patient Result PDF</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; color: #1f2937; font-size: 12px; margin: 32px 28px; }
        .brand { width: 100%; border-bottom: 2px solid #0f766e; padding-bottom: 12px; margin-bottom: 18px; }
        .brand td { vertical-align: top; }
        .logo { width: 160px; max-height: 56px; object-fit: contain; }
        .title { font-size: 22px; font-weight: 700; margin: 0; }
        .subtitle { margin: 4px 0 0; color: #4b5563; }
        .meta { text-align: right; font-size: 10px; }
        .card { border: 1px solid #dbe4ee; border-radius: 10px; padding: 12px 14px; margin-bottom: 14px; }
        .grid { width: 100%; }
        .grid td { width: 50%; padding: 6px 0; vertical-align: top; }
        .label { font-size: 10px; text-transform: uppercase; letter-spacing: 0.08em; color: #6b7280; display: block; margin-bottom: 3px; }
        .value { font-size: 14px; font-weight: 700; }
        .high { color: #b91c1c; }
        .good { color: #047857; }
    </style>
</head>
<body>
    <table class="brand">
        <tr>
            <td style="width: 180px;">
                <img src="{{ public_path('images/vilocarelogo.png') }}" alt="ViLoCare Logo" class="logo">
            </td>
            <td>
                <p class="title">Patient Viral Load Result</p>
                <p class="subtitle">Official PDF copy generated by ViLoCare.</p>
            </td>
            <td class="meta">
                <div>Generated {{ now()->format('d M Y H:i') }}</div>
                <div>Patient copy</div>
            </td>
        </tr>
    </table>

    <div class="card">
        <table class="grid">
            <tr>
                <td>
                    <span class="label">ART Number</span>
                    <span class="value">{{ $patient->art_number }}</span>
                </td>
                <td>
                    <span class="label">Patient Name</span>
                    <span class="value">{{ $patient->first_name }} {{ $patient->last_name }}</span>
                </td>
            </tr>
            <tr>
                <td>
                    <span class="label">Sex</span>
                    <span class="value">{{ $patient->sex }}</span>
                </td>
                <td>
                    <span class="label">Phone</span>
                    <span class="value">{{ $patient->phone ?: 'N/A' }}</span>
                </td>
            </tr>
        </table>
    </div>

    <div class="card">
        <table class="grid">
            <tr>
                <td>
                    <span class="label">Sample Date</span>
                    <span class="value">{{ $result->sample_date ?: 'N/A' }}</span>
                </td>
                <td>
                    <span class="label">Result Date</span>
                    <span class="value">{{ $result->result_date ?: 'N/A' }}</span>
                </td>
            </tr>
            <tr>
                <td>
                    <span class="label">Result (copies/ml)</span>
                    <span class="value">{{ $result->result_cpml !== null ? number_format((float) $result->result_cpml, 2) : 'N/A' }}</span>
                </td>
                <td>
                    <span class="label">Result (log)</span>
                    <span class="value">{{ $result->result_log !== null ? number_format((float) $result->result_log, 2) : 'N/A' }}</span>
                </td>
            </tr>
            <tr>
                <td>
                    <span class="label">Clinical Status</span>
                    <span class="value {{ ($result->result_cpml !== null && $result->result_cpml >= 1000) ? 'high' : 'good' }}">
                        {{ ($result->result_cpml !== null && $result->result_cpml >= 1000) ? 'High viral load - EAC required' : 'Suppressed / stable' }}
                    </span>
                </td>
                <td>
                    <span class="label">Testing Indication</span>
                    <span class="value">{{ $result->vl_testing_indication ?: 'N/A' }}</span>
                </td>
            </tr>
        </table>
    </div>
</body>
</html>
